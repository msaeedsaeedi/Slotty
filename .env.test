# Separate database for integration/e2e tests (created by infra/postgres/init.sql).
# Test helpers refuse to wipe any database whose name isn't slotty_test.
DATABASE_URL="postgres://slotty:slotty@localhost:5432/slotty_test"
APP_URL="http://localhost:3100"
SMTP_HOST=""
